# Your existing content for Prefect Dockerfile goes here.
# Example (adjust based on your actual Prefect Dockerfile):
# FROM python:3.9-slim-buster
# WORKDIR /app
# COPY requirements_prefect.txt .
# RUN pip install -r requirements_prefect.txt
# ... other Prefect setup commands ...

# Add this at the very end of your Dockerfile.prefect
CMD ["prefect", "worker", "start", "--pool", "default-docker-pool", "--type", "docker"]
