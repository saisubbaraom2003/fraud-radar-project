# Your existing content for MLflow Dockerfile goes here.
# Example (adjust based on your actual MLflow Dockerfile):
# FROM python:3.9-slim-buster
# WORKDIR /app
# COPY requirements_mlflow.txt .
# RUN pip install -r requirements_mlflow.txt
# ... other MLflow setup commands ...

# Add this at the very end of your Dockerfile.mlflow
CMD ["mlflow", "server", "--host", "0.0.0.0", "--port", "5000", "--backend-store-uri", "$(DATABASE_URL)", "--default-artifact-root", "s3://$(MLFLOW_ARTIFACT_BUCKET_NAME)/mlruns/"]